name: Kotlin CI

on:
  push:
    branches:
      - '**'
      - '!master'

# triggerBuildPipeline = 1

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: '0'

      - name: Set up GIT
        run: |
          git config user.name "$GITHUB_ACTOR"
          git config user.mail jan-hendrik.hausner@outlook

      - name: Set up JDK 11
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '11'
          cache: 'gradle'

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 16
          cache: 'npm'
          registry-url: 'https://npm.pkg.github.com'

      - name: Build with Gradle
        run: ./gradlew build --no-daemon
        env:
          GITHUB_AUTHORIZATION_TOKEN: ${{ secrets.API_GITHUB_AUTHORIZATION_TOKEN }}

      - name: Update Docs
        uses: EndBug/add-and-commit@v9
        with:
          add: './docs/bamconfigbackend.json'

      - name: Bump version and push tag
        uses: anothrNick/github-tag-action@1.36.0
        id: tag-version
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          DEFAULT_BUMP: minor
          RELEASE_BRANCHES: master
          PRERELEASE_SUFFIX: beta

      - name: Update Typescript package version
        run: npm --prefix ./build/${{ github.repository }}/typescript version ${{ steps.tag-version.outputs.new_tag }}

      - name: Publish Typescript beta library
        run: npm --prefix ./build/${{ github.repository }}/typescript publish

      - name: Publish Kotlin beta library
        run: ./gradlew -p build/${{ github.repository }}/kotlin build --no-daemon
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          DATA_MODEL_VERSION: ${{ steps.tag-version.outputs.new_tag }}

      - name: Build Docker Image
        run: docker build . --file Dockerfile --tag ${{ github.repository }}:latest